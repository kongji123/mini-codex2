# 验证策略

验证应基于项目事实和证据，而不是基于固定语言模板。

1. 优先使用项目已有脚本和文档化命令。
2. 选择能证明变更行为的最窄命令。
3. 语法/构建检查是有用门禁，但面向用户的行为通常需要 API、CLI、浏览器或工作流证据。
4. 如果自动验证与变更文件无关，检查附近项目元数据并选择更合适的命令。
5. 如果测试需要服务或数据，创建最小可控状态，而不是反复跑昂贵的端到端流程。
6. 将成功且稳定的验证命令记录为 workflow memory。

不要在这里写死语言行为。用项目文件决定验证命令。
