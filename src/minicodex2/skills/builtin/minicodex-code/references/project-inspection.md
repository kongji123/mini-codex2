# 项目探测

先使用项目事实，再做推断。

1. 从相关邻近文件、README、AGENTS.md、设计文档、包元数据和已有脚本开始。
2. 先用浅层 `list_directory`；只有目录形状本身重要时才递归列目录。
3. 查找符号、路由、配置键、错误字符串和文件名时使用 `search_files`。
4. 编辑前读取精确文件或行范围。
5. 如果启动方式或服务拓扑不清楚，优先调用 `inspect_project_startup`，再查看它报告的候选文件。

不要把语言或框架名称变成写死行为。让项目文件和已观察到的命令决定这个项目如何工作。
